<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Starter Website" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Starter Website">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= yield :head %>
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-white text-gray-900 min-h-screen flex flex-col">
    <nav class="bg-white border-b border-gray-200 sticky top-0 z-50">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16 items-center">
          <div class="flex items-center">
            <%= link_to root_path, class: "text-xl font-bold text-gray-900 hover:text-indigo-600 transition-colors" do %>
              Starter Website
            <% end %>
          </div>
          <div class="flex items-center space-x-8">
            <%= link_to "Home", root_path, class: "text-sm font-medium text-gray-700 hover:text-indigo-600 transition-colors #{request.path == '/' ? 'text-indigo-600' : ''}" %>
            <%= link_to "Podcasts", podcasts_path, class: "text-sm font-medium text-gray-700 hover:text-indigo-600 transition-colors #{request.path.start_with?('/podcasts') ? 'text-indigo-600' : ''}" %>
            <%= link_to "Blog", blog_path, class: "text-sm font-medium text-gray-700 hover:text-indigo-600 transition-colors #{request.path.start_with?('/blog') ? 'text-indigo-600' : ''}" %>
            <%= link_to "Book a Call", book_path, class: "inline-flex items-center px-4 py-2 bg-indigo-600 text-white text-sm font-medium rounded-lg hover:bg-indigo-700 transition-colors" %>
          </div>
        </div>
      </div>
    </nav>

    <% if notice.present? %>
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3 rounded-lg">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded-lg">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <main class="flex-1">
      <%= yield %>
    </main>

    <footer class="bg-gray-50 border-t border-gray-200 mt-16">
      <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-8">
        <div class="text-center text-sm text-gray-500">
          &copy; <%= Date.current.year %> Starter Website. All rights reserved.
        </div>
      </div>
    </footer>
  </body>
</html>
